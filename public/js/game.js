// game.js
export function startGame(
   targetTextElement,
   userInputElement,
   cpmElement,
   accuracyElement,
   timerElement
) {
   // Запуск игры
}

export function handleInput(
   targetTextElement,
   userInputElement,
   cpmElement,
   accuracyElement,
   timerElement
) {
   // Обработка ввода пользователя
}

export function calculateWPM(startTime, endTime, typedChars) {
   // Подсчет скорости
}

export function calculateAccuracy(targetText, inputText) {
   // Подсчет точности
}

export function updateTimer(timerElement, timeLeft) {
   // Обновление таймера
}
